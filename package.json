{
  "name": "social-mentor",
  "version": "1.0.0",
  "description": "Social Mentor Platform - Donation and Volunteer Management",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "echo Run 'npm run frontend' and 'npm run backend' in separate terminals"
  }
}
